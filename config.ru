# This file is used by Rack-based servers to start the application.

require_relative 'config/environment'

#require "#{RAILS.root}/app/classes/generate_new_week_assignments.rb"

run Rails.application
